{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://draft.dev/schemas/draft-global-home.schema.json",
  "title": "Draft Global Home",
  "description": "The global user store's identity marker (home.json). Written once, atomically, only by the call that established the store root; never rotated. The sole positive proof that a directory is a Draft-managed store.",
  "type": "object",
  "required": ["schema_version", "store_id", "created_at"],
  "properties": {
    "schema_version": { "const": 1 },
    "store_id": { "type": "string", "pattern": "^gst_[0-9a-f]{12}$" },
    "created_at": { "type": "string", "format": "date-time" }
  },
  "additionalProperties": false
}
