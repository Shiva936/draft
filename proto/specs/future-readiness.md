# Future Readiness

v0.3.4 does not implement hosted review, CI/CD, remote APIs, or remote runners. Publication to an external provider is local, synchronous and optional; asynchronous providers are out of scope for v1. Local receipts, digests and Baseline metadata are deterministic and portable, so later releases can add those systems without replacing local contracts.
