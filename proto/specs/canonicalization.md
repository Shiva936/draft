# Canonicalization Protocol

Canonical JSON uses sorted object keys, stable array order, compact separators,
normalized workspace-relative paths, and SHA-256 hashes prefixed with `sha256:`.

Canonicalization applies to changepacks, receipts, events, project states,
stable-head metadata, compositions, config, and verification keys.
