[package]
name = "draft-console-application"
version = "0.3.4"
edition = "2021"

[dependencies]
draft-ipc = { path = "../../services/ipc" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3"
