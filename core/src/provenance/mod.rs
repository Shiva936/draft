//! Where a derived fact came from.
//!
//! Derivation records name the inputs a derived value was computed from, so a
//! reader can tell a fact that was established from one that was assumed.

pub mod derived;
