# Reviewed exceptions to the boundary checks in check-extension-architecture.sh.
#
# Every line is `path:pattern`, matched as a plain substring against the
# offending line's `file:text`. Keep this list short: each entry is a place the
# boundary is deliberately not enforced, and needs a reason.

# --- Pre-existing `provider` vocabulary -------------------------------------
# These predate the extension boundary work and mean "identity provider" or
# "version-control control directory" — neither is an extension capability.
# The check exists to stop *new* uses of the word, because the public docs ban
# it outright (cli/tests/smoke.rs::docs_do_not_use_retired_external_action_terms).
core/src/trust/identity/global.rs:provider

# --- Draft's own build instructions ------------------------------------------
# These name Draft's build commands in a generated-file header or a comment.
# They are instructions for maintaining this repository, not domain knowledge
# Draft acts on, and nothing branches on them.
services/ipc/src/console_contracts.rs:@generated by cargo run -p draft-ipc
console/web/src/styles.css:generated from theme/tokens.json by `npm run tokens`
console/web/src/theme/tokens.json:generated from this file by `npm run tokens`


# --- Frozen contract field names in a test fixture ---------------------------
# `core::promotion::coverage` decides whether coverage is sufficient. It reads
# the SDK's `CoverageEvidence` — a frozen contract type — so its tests must
# build one, and building one means spelling the field and type names the
# contract already froze. This is not Core inventing vocabulary; it is Core
# naming somebody else's. The prose in this file says "binding" throughout.
core/src/promotion/coverage.rs:ProviderBindingId
core/src/promotion/coverage.rs:ProviderSemanticDefinitionDigest

# --- Frozen contract names in the §2.46 cross-record test fixture ------------
# `core::publication::consistency` checks a PublicationAttempt against the
# records outside it, so its tests must build a complete attempt and a complete
# Publication. Both are frozen SDK types whose field and type names the
# contract already fixed — `route`, `provenance`, the binding generation the
# dispatch was validated at. Core is naming somebody else's vocabulary here,
# not minting its own: the module's own prose says "external system" and
# "binding" throughout, and none of these names appears outside the test
# fixture.
core/src/publication/consistency.rs:draft_dcg_contract::provider
core/src/publication/consistency.rs:ProviderOperationalProfileDigest
core/src/publication/consistency.rs:ProviderProvenanceRef
core/src/publication/consistency.rs:ProviderRouteRef
core/src/publication/consistency.rs:ProviderBindingGeneration
core/src/publication/consistency.rs:ProviderBindingId
core/src/publication/consistency.rs:ProviderSemanticDefinitionDigest

# --- Frozen read-model vocabulary ------------------------------------------
# `CurrentProviderRoutability` is the architecture's own name for "can current
# configuration reach this target", paired against `HistoricalBaselineComposition`;
# `StoreKey::ProviderBinding` names the binding store whose generation a view
# depends on. Both are the frozen model being referred to, not new vocabulary
# Core is minting — the surrounding prose says "binding" and "target".
core/src/read_model/freshness.rs:ProviderBinding
core/src/read_model/baseline.rs:CurrentProviderRoutability
core/src/read_model/baseline.rs:ProviderBinding
core/src/read_model/baseline.rs:ProviderProvenanceRef
core/src/read_model/baseline.rs:ProviderRouteRef
core/src/read_model/baseline.rs:ProviderOperationalProfileDigest
core/src/read_model/baseline.rs:ProviderSemanticDefinitionDigest
core/src/read_model/baseline.rs:ProviderKindId
core/src/read_model/baseline.rs:draft_dcg_contract::provider

# --- Frozen provider-model names in the observation and acceptance chain ----
# An Observation records which binding established a Resource's state, so these
# modules carry `ProviderBindingId` and `ProviderSemanticDefinitionDigest`
# through to the Baseline roots. They are the frozen contract's field and type
# names, not vocabulary Core is minting: the prose in each file says "binding".
core/src/dcg/observation_set.rs:ProviderBindingId
core/src/dcg/observation_set.rs:ProviderSemanticDefinitionDigest
core/src/dcg/observation_set.rs:provider_binding
core/src/dcg/observation_set.rs:provider_semantic_definition
core/src/dcg/observe.rs:ProviderBindingId
core/src/dcg/observe.rs:ProviderSemanticDefinitionDigest
core/src/dcg/observe.rs:provider_binding
core/src/dcg/observe.rs:provider_semantic_definition
core/src/dcg/observe.rs:filesystem_provider
core/src/dcg/observation_set.rs:filesystem_provider
core/src/dcg/accept.rs:ProviderSemanticDefinitionDigest
core/src/dcg/accept.rs:filesystem_provider
core/src/app/baseline.rs:ProviderBindingStore
core/src/app/baseline.rs:ProviderDefinitionStore
core/src/app/baseline.rs:ProviderBinding
core/src/app/baseline.rs:filesystem_provider
core/src/app/baseline.rs:provider_binding
core/src/app/baseline.rs:provider_semantic_definition

# --- Frozen provider-model names on the publication route --------------------
# A Publication carries a `ProviderRouteRef`, and constructing one means naming
# the frozen contract's types and the on-disk directory the bindings live in.
# The prose in both files says "binding" throughout; these are somebody else's
# names being spelled, not vocabulary Core is minting.
core/src/app/publish.rs:ProviderProvenanceRef
core/src/app/publish.rs:ProviderRouteRef
core/src/app/publish.rs:ProviderBindingStore
core/src/app/publish.rs:draft_dcg_contract::provider
core/src/app/publish.rs:crate::project::provider
core/src/app/workflow.rs:ProviderProvenanceRef
core/src/app/workflow.rs:draft_dcg_contract::provider
core/src/dcg/accept.rs:ProviderProvenanceRef
core/src/dcg/accept.rs:provider_binding
core/src/dcg/accept.rs:provider_semantic_definition

# --- Frozen provider-model names in Phase 1 authority capture ---------------
# `publication/authority.rs` validates the frozen `ProviderRouteRef` against the
# binding's current pointers and records the binding's generation in the
# attempt. Those are the contract's own type names and the on-disk directory
# the bindings live in; the prose in the file says "binding" throughout.
core/src/publication/authority.rs:ProviderBindingStore
core/src/publication/authority.rs:ProviderRouteRef
core/src/publication/authority.rs:ProviderBindingGeneration
core/src/publication/authority.rs:crate::project::provider
core/src/publication/authority.rs:draft_dcg_contract::provider
core/src/publication/dispatch.rs:draft_dcg_contract::provider
core/src/publication/dispatch.rs:provider_binding_generation_at_dispatch

# --- The frozen §2.57 counter name -------------------------------------------
# `provider_binding_cas_conflicts` is one of the frozen operational counter
# names. An operator's dashboards are written against it, so it is spelled the
# way the contract froze it rather than renamed here; the surrounding prose says
# "binding". The registry mints no vocabulary of its own.
core/src/support/telemetry.rs:ProviderBindingCasConflicts
core/src/support/telemetry.rs:provider_binding_cas_conflicts

# --- The freshness watermark reads the binding store --------------------------
# §2.56 lists a `ProviderBinding` generation change as invalidating current
# configuration, routability and publication eligibility, so the watermark has
# to read that store to answer whether a view is stale. These are the store's
# own type name and its on-disk directory; the prose in the file says "binding"
# throughout, and the projection variant is named `CurrentRoutability`.
core/src/read_model/freshness.rs:ProviderBindingStore
core/src/read_model/freshness.rs:Provider bindings, publication controls and tasks are
# The projection that names the binding store, and the test that proves it
# tracks that store and nothing else. `ProviderCatalog` is the frozen §2.56
# row's own subject; the test is what stops it quietly growing a dependency.
core/src/read_model/freshness.rs:ProviderCatalog
core/src/read_model/freshness.rs:This project's provider bindings, definitions and profiles.

# --- A Baseline's composition names the frozen provenance type ----------------
# §8.3's Baseline scope renders `accepted_provider_provenance`, so this view
# carries `ProviderProvenanceRef` — the SDK's own frozen type. It is naming the
# contract, not minting domain vocabulary, and the surrounding prose says
# "binding" and "composition" throughout.
core/src/app/baseline_detail.rs:use draft_dcg_contract::provider::ProviderProvenanceRef;
core/src/app/baseline_detail.rs:pub composition: BTreeMap<ResourceId, ProviderProvenanceRef>,

# --- Provider binding lifecycle at the application boundary -------------------
# `app/provider.rs` is the command surface for attaching a project to an
# external system: it names `ProviderSemanticDefinition`, `ProviderOperationalProfile`
# and `ProviderBinding` because those are the frozen contract's own types, and
# it is the one place a binding's pointer or lifecycle moves. No vocabulary is
# minted here — every name is spelled the way the contract froze it.
core/src/app/provider.rs:Provider
core/src/app/provider.rs:provider
core/src/app/mod.rs:provider_catalog
core/src/app/mod.rs:provider_list
core/src/app/mod.rs:provider_show
core/src/app/mod.rs:provider_bind
core/src/app/mod.rs:provider_redefine
core/src/app/mod.rs:provider_profile
core/src/app/mod.rs:provider_unbind
core/src/app/mod.rs:provider_rebind
core/src/app/mod.rs:crate::app::provider
core/src/app/mod.rs:crate::project::provider
core/src/app/mod.rs:ProviderBindingId
core/src/app/mod.rs:ProviderOperationalProfile
core/src/app/mod.rs:ProviderSemanticDefinition
core/src/app/mod.rs:ProviderBinding
core/src/app/mod.rs:pub mod provider;
core/src/app/mod.rs:accepted_provider_provenance
core/src/app/mod.rs:Every provider binding this project has
core/src/app/mod.rs:One provider binding, with the immutable facts
core/src/app/mod.rs:Attach this project to a provider.
core/src/app/roots.rs:the contract outlives every provider
core/src/app/roots.rs:it may name an attempt that reached a provider.
core/src/publication/barrier.rs:a pending external resolution needs the provider back
core/src/app/roots.rs:provider-binding
core/src/app/roots.rs:provenance.binding


# --- The layout is the sole authoritative map -------------------------------
# Every store's directory is named once, here, so a reader asking where a
# provider binding lives has one answer. The provider-model directory names
# are the frozen §4 layout's own; nothing else in Core spells them.
core/src/project/layout.rs:provider-bindings
core/src/project/layout.rs:provider-definitions
core/src/project/layout.rs:provider_bindings_dir
core/src/project/layout.rs:provider_definitions_dir
core/src/project/layout.rs:Provider bindings: revisioned pointers
core/src/project/layout.rs:The immutable semantic definitions and operational profiles bindings
core/src/project/layout.rs:point at. Retained forever: a Baseline's provenance names one.
